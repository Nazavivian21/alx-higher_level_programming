#!/usr/bin/python3
def inherits_from(obj, a_class):
	issubclass(a_class, obj)
"""
while obj_class is not None:
  if obj_class == a_class:
		return True
    obj_class = obj_class.se__
        
return False
"""